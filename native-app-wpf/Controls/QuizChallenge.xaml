<UserControl x:Class="CodeTutor.Wpf.Controls.QuizChallenge"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:CodeTutor.Wpf.Controls"
             xmlns:behaviors="clr-namespace:CodeTutor.Wpf.Behaviors">
    <Grid>
        <Border x:Name="QuizCard"
                Style="{StaticResource CardBorder}"
                Margin="0,0,0,24"
                behaviors:AnimationBehaviors.EnableFadeInOnLoad="True">
            <StackPanel>
                <!-- Quiz Header -->
                <TextBlock x:Name="QuizTitle"
                           Style="{StaticResource SubheadingText}"
                           Margin="0,0,0,8" />
                <TextBlock x:Name="QuizDescription"
                           Style="{StaticResource BodyText}"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,0,0,16" />

                <!-- Question -->
                <Border Background="{StaticResource BackgroundLightBrush}"
                        CornerRadius="6"
                        Padding="16"
                        Margin="0,0,0,16">
                    <TextBlock x:Name="QuestionText"
                               Style="{StaticResource BodyText}"
                               TextWrapping="Wrap"
                               FontWeight="Medium" />
                </Border>

                <!-- Answer Options -->
                <StackPanel x:Name="OptionsPanel" Margin="0,0,0,16" />

                <!-- Submit Button -->
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <Button x:Name="SubmitButton"
                            Style="{StaticResource PrimaryButton}"
                            Click="Submit_Click"
                            IsEnabled="False">
                        <TextBlock Text="Submit Answer" />
                    </Button>
                    <Button x:Name="ResetButton"
                            Style="{StaticResource GhostButton}"
                            Click="Reset_Click"
                            Margin="12,0,0,0"
                            Visibility="Collapsed">
                        <TextBlock Text="Try Again" />
                    </Button>
                </StackPanel>

                <!-- Result Panel -->
                <Border x:Name="ResultPanel"
                        CornerRadius="6"
                        Padding="16"
                        Margin="0,0,0,0"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="ResultText"
                                   Style="{StaticResource BodyText}"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,8" />
                        <TextBlock x:Name="ExplanationText"
                                   Style="{StaticResource BodyText}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <controls:ConfettiOverlay x:Name="ConfettiOverlay"
                                  Visibility="Collapsed"
                                  Panel.ZIndex="2" />
        <controls:AchievementBadge x:Name="AchievementBadge"
                                   HorizontalAlignment="Right"
                                   VerticalAlignment="Top"
                                   Margin="0,12,12,0"
                                   Panel.ZIndex="3" />
    </Grid>
</UserControl>
